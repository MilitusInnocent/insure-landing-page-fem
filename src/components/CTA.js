import React from 'react';
import { Button } from './';

const CTA = () => (
    <div className="cta">
        <h2 className="cta__heading">Find out more about how we work</h2>
        <Button color='gray' text='how we work' />
    </div>
)

export default CTA;